%% this is a .hrl (header) file.
-record(journalItemQmo, {journalItem,
				                   name,
				                   description,
				                   salesrelation,
				                   blocked,
				                   deadline}).